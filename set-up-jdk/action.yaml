---
name: Set Up JDK
description: Set up jdk
inputs:
  java-version:
    description: Java version
    required: false
    default: "17"
runs:
  using: "composite"
  steps:
    - uses: actions/setup-java@b36c23c0d998641eff861008f374ee103c25ac73 # pin@v3
      with:
        java-version: ${{ inputs.java-version }}
        distribution: "adopt"
        cache: "maven"
        server-id: github
        server-username: GITHUB_USER_REF
        server-password: GITHUB_TOKEN_REF
