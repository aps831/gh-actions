---
name: Install xmlstartlet
description: Install xmlstartlet
runs:
  using: "composite"
  steps:
    - id: install-xmlstarlet
      shell: bash
      run: |
        sudo apt-get update
        sudo apt-get install xmlstarlet
