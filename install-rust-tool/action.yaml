---
name: Install Rust Tool
description: Install Rust tool
inputs:
  tool:
    description: Tool including version to install
    required: true
runs:
  using: "composite"
  steps:
    - uses: taiki-e/install-action@56ab7930c591507f833cbaed864d201386d518a8 # pin@v2
      with:
        tool: ${{ inputs.tool }}
