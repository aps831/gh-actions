---
name: Install Rust Tool
description: Install Rust tool
inputs:
  tool:
    description: Tool including version to install
    required: true
runs:
  using: "composite"
  steps:
    - uses: taiki-e/install-action@2a505d476c2be89444ffaf18cd840761320cc917 # pin@v2
      with:
        tool: ${{ inputs.tool }}
