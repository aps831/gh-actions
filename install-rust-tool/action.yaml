---
name: Install Rust Tool
description: Install Rust tool
inputs:
  tool:
    description: Tool including version to install
    required: true
runs:
  using: "composite"
  steps:
    - uses: taiki-e/install-action@f172798ba087526822a4f43dd08714d08a45aa37 # pin@v2
      with:
        tool: ${{ inputs.tool }}
