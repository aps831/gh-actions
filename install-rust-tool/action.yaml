---
name: Install Rust Tool
description: Install Rust tool
inputs:
  tool:
    description: Tool including version to install
    required: true
runs:
  using: "composite"
  steps:
    - uses: taiki-e/install-action@37129d5de13e9122cce55a7a5e7e49981cef514c # pin@v2
      with:
        tool: ${{ inputs.tool }}
