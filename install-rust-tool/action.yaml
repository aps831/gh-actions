---
name: Install Rust Tool
description: Install Rust tool
inputs:
  tool:
    description: Tool including version to install
    required: true
runs:
  using: "composite"
  steps:
    - uses: taiki-e/install-action@0d6353d3dc1eac4359c520185c44119bfbd812b5 # pin@v2
      with:
        tool: ${{ inputs.tool }}
