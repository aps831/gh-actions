---
name: Install Rust Tool
description: Install Rust tool
inputs:
  tool:
    description: Tool including version to install
    required: true
runs:
  using: "composite"
  steps:
    - uses: taiki-e/install-action@4f8e32492b3baed061f7836e6a4d40eb19e49b71 # pin@v2
      with:
        tool: ${{ inputs.tool }}
