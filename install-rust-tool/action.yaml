---
name: Install Rust Tool
description: Install Rust tool
inputs:
  tool:
    description: Tool including version to install
    required: true
runs:
  using: "composite"
  steps:
    - uses: taiki-e/install-action@a7eed8c9d8a574f9973ac425db6e20a115e5d64e # pin@v2
      with:
        tool: ${{ inputs.tool }}
