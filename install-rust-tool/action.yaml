---
name: Install Rust Tool
description: Install Rust tool
inputs:
  tool:
    description: Tool including version to install
    required: true
runs:
  using: "composite"
  steps:
    - uses: taiki-e/install-action@05515455a9007c7c6f0b215637566ab8e9872c12 # pin@v2
      with:
        tool: ${{ inputs.tool }}
