---
name: Install Rust Tool
description: Install Rust tool
inputs:
  tool:
    description: Tool including version to install
    required: true
runs:
  using: "composite"
  steps:
    - uses: taiki-e/install-action@60784cb1f434ce8a3cc04670ea753e1c8868e17e # pin@v2
      with:
        tool: ${{ inputs.tool }}
