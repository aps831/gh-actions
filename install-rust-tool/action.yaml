---
name: Install Rust Tool
description: Install Rust tool
inputs:
  tool:
    description: Tool including version to install
    required: true
runs:
  using: "composite"
  steps:
    - uses: taiki-e/install-action@6ad9974df998e38cabfa03b4cedaade74e8e659e # pin@v2
      with:
        tool: ${{ inputs.tool }}
