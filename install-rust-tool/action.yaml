---
name: Install Rust Tool
description: Install Rust tool
inputs:
  tool:
    description: Tool including version to install
    required: true
runs:
  using: "composite"
  steps:
    - uses: taiki-e/install-action@437c908c7e5ee18b63a261286cbe5147219f8a39 # pin@v2
      with:
        tool: ${{ inputs.tool }}
