---
name: Install Rust Tool
description: Install Rust tool
inputs:
  tool:
    description: Tool including version to install
    required: true
runs:
  using: "composite"
  steps:
    - uses: taiki-e/install-action@00a67321d66e038602baf558d366a594a7019ea2 # pin@v2
      with:
        tool: ${{ inputs.tool }}
