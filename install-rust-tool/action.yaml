---
name: Install Rust Tool
description: Install Rust tool
inputs:
  tool:
    description: Tool including version to install
    required: true
runs:
  using: "composite"
  steps:
    - uses: taiki-e/install-action@99774fec7fd4f75144bd0134a24a992297768308 # pin@v2
      with:
        tool: ${{ inputs.tool }}
