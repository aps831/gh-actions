---
name: Install Rust Tool
description: Install Rust tool
inputs:
  tool:
    description: Tool including version to install
    required: true
runs:
  using: "composite"
  steps:
    - uses: taiki-e/install-action@e17a4e247d4a36441181d7758c499d97e1e006bd # pin@v2
      with:
        tool: ${{ inputs.tool }}
