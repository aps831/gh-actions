---
name: Install Rust Tool
description: Install Rust tool
inputs:
  tool:
    description: Tool including version to install
    required: true
runs:
  using: "composite"
  steps:
    - uses: taiki-e/install-action@32300fcc7462d35c920c6d4a42efe7bc39b61569 # pin@v2
      with:
        tool: ${{ inputs.tool }}
