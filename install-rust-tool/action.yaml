---
name: Install Rust Tool
description: Install Rust tool
inputs:
  tool:
    description: Tool including version to install
    required: true
runs:
  using: "composite"
  steps:
    - uses: taiki-e/install-action@22c308f3b9941592880ec172b34343348e289027 # pin@v2
      with:
        tool: ${{ inputs.tool }}
