---
name: Install Rust Tool
description: Install Rust tool
inputs:
  tool:
    description: Tool including version to install
    required: true
runs:
  using: "composite"
  steps:
    - uses: taiki-e/install-action@51767789bcd596c475bee2e8b0a6a8fc191f66d8 # pin@v2
      with:
        tool: ${{ inputs.tool }}
