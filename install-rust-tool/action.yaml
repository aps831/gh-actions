---
name: Install Rust Tool
description: Install Rust tool
inputs:
  tool:
    description: Tool including version to install
    required: true
runs:
  using: "composite"
  steps:
    - uses: taiki-e/install-action@b7add58e53e52e624966da65007ce24524f3dcf3 # pin@v2
      with:
        tool: ${{ inputs.tool }}
