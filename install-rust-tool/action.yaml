---
name: Install Rust Tool
description: Install Rust tool
inputs:
  tool:
    description: Tool including version to install
    required: true
runs:
  using: "composite"
  steps:
    - uses: taiki-e/install-action@da8fe73ed87107a1cae164305a928b7c8fcff4bc # pin@v2
      with:
        tool: ${{ inputs.tool }}
