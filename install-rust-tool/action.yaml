---
name: Install Rust Tool
description: Install Rust tool
inputs:
  tool:
    description: Tool including version to install
    required: true
runs:
  using: "composite"
  steps:
    - uses: taiki-e/install-action@672949f0e99e12c1a48510fa548e812f6835397f # pin@v2
      with:
        tool: ${{ inputs.tool }}
