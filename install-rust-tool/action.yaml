---
name: Install Rust Tool
description: Install Rust tool
inputs:
  tool:
    description: Tool including version to install
    required: true
runs:
  using: "composite"
  steps:
    - uses: taiki-e/install-action@6943331e01261cdff7420bbc2508cb463574e404 # pin@v2
      with:
        tool: ${{ inputs.tool }}
