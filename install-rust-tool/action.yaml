---
name: Install Rust Tool
description: Install Rust tool
inputs:
  tool:
    description: Tool including version to install
    required: true
runs:
  using: "composite"
  steps:
    - uses: taiki-e/install-action@678b06b887cdbf44fa0601e5915f865e17c2241d # pin@v2
      with:
        tool: ${{ inputs.tool }}
