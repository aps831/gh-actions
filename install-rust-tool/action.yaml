---
name: Install Rust Tool
description: Install Rust tool
inputs:
  tool:
    description: Tool including version to install
    required: true
runs:
  using: "composite"
  steps:
    - uses: taiki-e/install-action@ae888b48c8777229768754549e5463ba726cb1b3 # pin@v2
      with:
        tool: ${{ inputs.tool }}
